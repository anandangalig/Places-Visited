<html>
<head>
  <title>Place I Have Been</title>
</head>
<body>
  <div class="container">
    <h1>Travel Log</h1>

    {% if places is not empty %}
      <p>These are the places you have been:</p>
      <ul>
        {% for place in places %}
          <li>{{ place.getCity }}</li>
        {% endfor %}
      </ul>
    {% endif %}

    <form action='/createplace' method='post'>
      <label for='city_locale'>Enter a city you have visited:</label>
      <input id='city_locale' name='city_locale' type='text'>
      <button type='submit'>Add your places...</button>
    </form>
  </div>
</body>
</html>
